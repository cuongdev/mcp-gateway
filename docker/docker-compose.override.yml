services:
  mcp-gateway:
    ports: !override
      - "3001:3000"
      - "9091:9090"
    volumes: !override
      - ../.docker-config:/app/config:ro
      - ../.docker-data:/app/data
      - gateway-logs:/app/logs
    environment:
      - NODE_ENV=production
      - GATEWAY_PORT=3000
